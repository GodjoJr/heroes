/**
 * @typedef {keyof typeof DIRECTION} Direction
 */

/** @enum {Direction} */
export const DIRECTION = Object.freeze({
    UP: 'UP',
    DOWN: 'DOWN',
    LEFT: 'LEFT',
    RIGHT: 'RIGHT',
    NONE: 'NONE'
});